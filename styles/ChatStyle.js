import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  chatbotText: {
    color: "white",
    fontSize: 16,
    fontWeight: "300",
    margin: "10",
    textAlign: "center",
  },
});

export default styles;
